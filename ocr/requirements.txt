fastapi>=0.115,<0.125

uvicorn>=0.30,<0.35

pydantic>=2.10,<2.13

python-multipart>=0.0.18

pdf2image>=1.17.0

pypdf>=4.3.1

Pillow>=10.4.0

# OCR engines. Tesseract is the fallback and the source of the calibrated
# page-quality signal; PP-OCR (PP-OCRv6 weights executed on onnxruntime) is the
# primary engine. rapidocr ships the .onnx weights inside the wheel (~31 MB),
# so there is no model download at build time or on first request.
pytesseract>=0.3.13

rapidocr>=3.9,<4

onnxruntime>=1.18,<2

google-genai>=1.0.0

python-dotenv>=1.0.1
